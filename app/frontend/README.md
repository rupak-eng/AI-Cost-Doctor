# Frontend for AI Cost Doctor (Next.js 14 + Tailwind)

## Develop

```bash
npm install
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
```

## Build / production

```bash
npm run build
npm start
```

The compose `web` service builds this directory via `app/frontend/Dockerfile`.
